# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017
DB_NAME=bot_trainer_db

# JWT Configuration
JWT_SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=12

# Email Configuration for Password Reset (Optional - uses dev mode if not set)
# For Gmail: Use App Password (not your Gmail password)
# Enable 2FA and generate App Password: https://myaccount.google.com/apppasswords
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password

# Development Mode
# If SENDER_EMAIL and SENDER_PASSWORD are not set, OTP will be printed to console instead of sending email
